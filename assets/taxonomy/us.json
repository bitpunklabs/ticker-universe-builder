{
  "schema_version": 2,
  "market": "us",
  "extends": "_equity",
  "note": "The shared base unchanged. It was abstracted from this table, so the US delta is empty by construction — the weights in _equity.json are already the US reading."
}
